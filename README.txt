To load the page, go to the BookRecommend folder with the manage.py file. Then type "python manage.py runserver" in the terminal and paste the url into a web browser. 

Note that we only have limited data in the database right now, so maybe try "science" as keyword, "computer science" as industry, and "physics" as major? The database will start building up once people start using it.